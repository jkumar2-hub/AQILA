# AQILA — Frontend Pages Directory
# Owner: M3 — Frontend / UX
#
# PLACEHOLDER — M3 populates this directory during Hour 0 of the hackathon.
#
# Planned pages (per v4.1 §3 M3):
#   P0:
#     - UploadPage   — react-dropzone, per-file ingest card, progress bar
#     - QueryPage    — chat interface, answer + citation pills, contradiction banner
#     - EvidencePage — react-force-graph-2d, node click, side panel
#   P1:
#     - SourcesPage  — table: name, type, chunks, status, date; preview drawer; delete
#     - SettingsPage — Ollama status, model selector, clear DB
#
# Routing: React Router or TanStack Router (M3 decides at Hour 0)
# Sidebar + routing scaffold built at Hour 0–2.
#
# References:
#   - docs/API_CONTRACTS.md               — AQILAResponse, EvidenceGraph
#   - frontend/src/mocks/mock_response.json — development mock data
