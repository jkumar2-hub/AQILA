"""
backend/app/api/ingest.py
────────────────────────────────────────────────────────────────────────────
AQILA — Ingest API Router
Owner: M4 — Backend / Platform / Integration

PLACEHOLDER — Implementation begins at Hour 0 of the hackathon.

P0 Endpoints (must work by Hour 10):
  POST /api/ingest/upload   — trigger ingest as BackgroundTask, return source_id
  GET  /api/ingest/status/{id} — return status lifecycle

References:
  - docs/API_CONTRACTS.md
  - rag/parsers.py, rag/embedder.py  (M1 modules, called from here)
"""

# TODO (M4, Hour 0–5): Implement ingest router
